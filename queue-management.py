# Write a program  to take a name as input, add it to the end of the queue, and output the resulting list.

queue = ['Ana', 'John', 'Marcus', 'Jake']

# take an input
name = input()

# add the taken value to the end of the queue
queue.append(name)

# display the updated queue
print(queue)
